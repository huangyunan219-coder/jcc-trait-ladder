# 参与完善

## 反馈数据错误

请在 Issue 中给出游戏版本、羁绊或英雄名称、当前错误与预期结果，并附上公开公告链接或能说明问题的游戏内截图。反馈阵容问题时，可以附网页「复制当前条件链接」或「复制阵容」的内容。

国服与 TFT 的同名强化可能有不同数值，优先采用当前金铲铲国服证据；跨服资料需要明确标注。不要仅凭单人激活门槛就把羁绊归为独有羁绊。

## 修改代码或数据

1. Fork 后创建分支，在本地 HTTP 服务器打开 `dist/`。
2. 保持规则与界面分离；小改动优先使用现有原生模块，不需要引入构建框架。
3. 规则修复应补一个能复现错误的用例。数据更改同步更新来源、核对日期和相关测试。
4. 运行 `npm test`，并检查受影响的页面操作及手机宽度。
5. PR 说明具体问题、变化和验证结果。不要提交访问令牌、账号文件、浏览器状态或与工具无关的个人信息。

`s18.json` 中的 `baseId` 用于识别同一弈子的不同形态，`slots` 是人口开销，`traits` 的值是实际贡献人数；特殊值不能简单全部重置成 1。`countable` 决定天梯计分资格，`emblem` 决定转职是否在输入列表中出现。

请保留卡兹克在所有方案中被排除的产品约束。若改变产品范围，先说明用途并在独立讨论中确认。
